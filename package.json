{
  "name": "zmk-config-viewer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:tooltips && vite",
    "build:tooltips": "node scripts/build-tooltips.mjs site/tooltips.json",
    "build": "npm run build:tooltips && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.14"
  }
}